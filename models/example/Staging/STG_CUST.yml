version: 2
 {{ config(materialized='table') }}
 
sources:
  - name: raw_qwt
    database: QWT_DEV
    schema: RAW_DEV
    tables:
      - name: raw_customers
      - name: raw_products
      - name: raw_orders
      - name: raw_order_details
      - name: Raw_Employee
      - name: raw_offices
      - name: raw_shipments
      - name: RAW_SUPPLIERS

